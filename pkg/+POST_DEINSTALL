#!/bin/sh
# post-deinstall for pfSense-pkg-amneziawg (after files removed)
PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin:/usr/local/sbin
export PATH

if [ -f /usr/local/etc/rc.d/amneziawg ]; then
	/usr/local/etc/rc.d/amneziawg stop 2>/dev/null || true
fi

if [ -x /usr/sbin/sysrc ]; then
	/usr/sbin/sysrc amneziawg_enable=NO >/dev/null 2>&1 || true
fi

echo "pfSense-pkg-amneziawg: removed. AmneziaWG settings may still exist in config.xml; remove them from a backup if desired."

exit 0
